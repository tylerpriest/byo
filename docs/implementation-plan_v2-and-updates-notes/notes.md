Add a list of things that you need human input with (logging into MCP’s CLI’s etc)
Github, Vercel, Supabase, ShadCN, Playwright

Tests should be in test folder

documentation should be in /docs/documentation

CLAUDE.md in root

Install ShadCN MCP early so AI can use it to build the front end

Don't use /.eslintrc.cjs it is outofdate I think?'

At the end run a fresh audit for any issues, you have just built a full featured platform, makes sense to double check for errors, issues, outdated tech, etc